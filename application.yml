spring:
  profiles:
    active: -dev

---
#开环境
spring:
  profiles: dev
  application:
    name: microservice-config-dev


---
# 测试环境
spring:
  profiles: test
  application:
    name: microservice-config-test

#请以UTF-8的编码保存